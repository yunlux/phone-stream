# 更新日志

## v0.2.11 (2026-09-11)

### 修复
- 改为按**进程列表**检测流，pid 文件丢失时按钮状态也能反映真实情况；停止时强杀进程组（TERM→KILL），并在启动前清掉同名孤儿
- 同一台手机的**音频与麦克风互斥**（同机双音频采集会冲突）：开一个会自动停另一个
- 脚本改为**后台跑 scrcpy + `wait`**，停止/TERM 立即生效（旧版前台运行会推迟 TERM 陷阱，导致进程变孤儿、麦克风按钮像"卡死"）

## v0.2.10 (2026-09-11)

### 变更
- 投屏/相机窗口标题加上连接方式，如 `k80 screen (USB)` / `k90 camera (WiFi)`

## v0.2.9 (2026-09-11)

### 修复
- 选 **USB** 但手机实际没接 USB 时，流不再"点了没反应"——现在会自动回退到无线（`wifi` 仍强制无线）

## v0.2.8 (2026-09-11)

### 变更
- 屏幕 / 相机参数改为**下拉选项**，方便直接选择：
  - 屏幕尺寸 = 长边像素列表（`0`=原生）；相机尺寸 = `WxH` 列表
  - 帧率、码率、视频编解码（h264 / h265 / av1 / vp8 / vp9）也都可选

## v0.2.7 (2026-09-11)

### 变更
- 设置界面改为**分页**（通用 / 音频 / 麦克风 / 屏幕 / 相机 / 连接方式），不再是一长页
- 每路流都有独立参数，并按 **WiFi** 与 **USB** 两列分别配置，实际取用与连接方式匹配的那组：
  - 音频 / 麦克风：延迟、编解码、码率
  - 屏幕 / 相机：尺寸、帧率、码率、视频编解码

### 新增
- `phone_video_scrcpy` 支持可选参数 `size`、`fps`、`bitrate (Mbps)`、`codec`

## v0.2.6 (2026-09-11)

### 新增
- 每台设备设置里可为**每路流**选择 USB / WiFi ADB（`auto` / `wifi` / `usb`）；检测到 USB 时卡片显示 `· USB`
- 脚本支持 `usb:<序列号>` 目标，直接用 USB 设备（不做 mDNS 发现）

### 变更
- 设备接在 USB 上时，adb 控制命令自动使用 USB 序列号

## v0.2.5 (2026-09-11)

### 移除
- 每张卡片的停止（⏹）按钮（对这些音视频流无意义）

## v0.2.4 (2026-09-11)

### 新增
- 每张卡片增加 ♥ 按钮：收藏当前曲目。优先通过手机端 MCP 触发媒体通知的"点赞/收藏"动作（`android_notification_list` + `android_notification_action`），失败则回退为 adb 展开通知栏点击

## v0.2.3 (2026-09-11)

### 变更
- 音频流节点命名为 `<设备> audio` / `<设备> mic`（如 `k80 audio`）；同时把 `application.name` 与 `node.description` 设为该名、`media.name` 置空，qpwgraph 就只显示这个名字（不再有 `scrcpy/... [scrcpy]`）

### 修复
- 节点名按"设备+角色"稳定后，qpwgraph 能记住节点位置（此前每次连接名字都变，新节点总被丢到很远的地方）

## v0.2.2 (2026-09-11)

### 新增
- 界面内语言切换：顶栏 **中文 / EN** 按钮；选择保存到 `settings.json`，下次启动仍生效

### 变更
- 语言取值顺序：已保存设置 > `PHONE_STREAM_LANG` > 英文

## v0.2.1 (2026-09-11)

### 变更
- 界面文本默认英文；设 `PHONE_STREAM_LANG=zh` 切中文
- 全部代码注释改为英文
- 文档默认英文，提供中文版（`README.zh-CN.md`、`CHANGELOG.zh-CN.md`）

### 修复
- 恢复音量 +/- 按钮（走相对音量键；在 `volume --set` 无效的机型上也可用）
- 音量滑条改为按差值发送音量键
- 轮询防叠加，修复视频暂停时卡片异常闪动

## v0.2.0 (2026-09-11)

### 新增
- 卡片式界面：每台设备一张卡片（设备信息 + 媒体信息 + 播放控制 + 设置）
- 麦克风 / 相机 / 屏幕 切换按钮（各自独立流，可与音频流共存）
- 设置项：名称、延迟、编解码、码率、音频源
- 音量滑条
- MCP 兜底：无 MediaSession 的应用（如小红书）用手机端 MCP 无障碍读屏取标题/作者；连不上时显示 `MCP offline`
- 关闭程序时自动断开所有连接（音频 / 麦克风 / 相机 / 屏幕）
- `sync-from-live.sh`：把实机脚本同步进仓库并自动通用化

### 变更
- 「连接 / 断开」合并为单个切换键
- 卡片按钮优先占位，窗口缩小时不再被裁切
- 播放控制按钮缩小，音量滑条移动到同一行

### 修复
- 设备地址优先使用扫描得到的实时地址（避免流缓存过期导致读不到媒体）
- 过滤 KDE Connect 的镜像会话（不再把 PC 端媒体误显示为手机媒体）

## v0.1.0

- 初始版本：mDNS 自动发现 / 手动添加设备、声音串流控制窗、通用 `phone_audio_scrcpy` / `phone_video_scrcpy`
